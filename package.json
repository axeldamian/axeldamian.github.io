{
  "homepage": "https://axeldamian.github.io",
  "devDependencies": {
    "gh-pages": "^6.1.1"
  },
  "scripts":{
    "build":"npm run build",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d build"
  }
}
